Add defined amount of products in a single dispatch (add 10)
and if added is greater than current quantity display alert and what is the major quantity

Cart page delete certain amount of elements which should not be more than currently added in cart

Check quantity of products entirely on client
Create /api/products/countinStock/[id] for just checking count in Stock
Prevent toastifier to be called multiple times
Make more efficient reducer
Compare reducer elements by id

change getServerSideProps from [slug] to [id]

Make database store cart if logged in
